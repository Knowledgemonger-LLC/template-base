---
description: Build the project using the command documented in AGENTS.md.
---

Build this project.

1. Read the **Build / Test / Run** section of `AGENTS.md` to find the exact build command.
2. Run it.
3. If it fails, diagnose the error and propose a fix. Do not guess at commands that are not
   documented in `AGENTS.md`.
4. Report the result concisely (success, or the failing step and why).
